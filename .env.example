# 大模型配置（以 OpenAI 为例）
OPENAI_API_KEY=sk-xxxxxxxxxxxx   # 替换为您的 OpenAI API Key
OPENAI_MODEL=gpt-3.5-turbo        # 可选模型

# 飞书机器人 Webhook（用于发送通知）
FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx

# 其他配置（如端口，部署平台会自动分配）
PORT=3000
