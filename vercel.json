{
  "builds": [
    {
      "src": "api/index.js",   // 如果主入口文件是 server.js 或 app.js，请相应修改
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.js"
    }
  ]
}
